/** Soft drop shadow shared by the cards across the app. */
export const CARD_SHADOW = {
  shadowColor: '#000',
  shadowOpacity: 0.3,
  shadowRadius: 18,
  shadowOffset: { width: 0, height: 10 },
  elevation: 3,
} as const;
